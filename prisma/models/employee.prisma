

model Employee {
  id          String   @id @default(auto()) @map("_id") @db.ObjectId
  customId    String   @unique
  name        String
  email       String   @unique
  phoneNumber String?
  status      Status
  designation String
  department  String
  user        User?    @relation

  @@map("employees")
}