model Department {
    id            String  @id @default(auto()) @map("_id") @db.ObjectId
    name          String  @unique
    contactPerson String
    phoneNumber   String?
    email         String  @unique
    buyer         Buyer?  @relation

    @@map("departments")
}
